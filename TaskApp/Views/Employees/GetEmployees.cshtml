@model TaskApp.Models.Employees

@{
    ViewBag.Title = "Employees";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

    <h2>Employees</h2>

    <p>@Html.ActionLink("New User", "NewEmployee", "Employees", null, new { @class = "btn btn-primary" })</p><br />

<table id="employees" class="table table-bordered table-hover">
    <thead>
        <tr>
            <th>Employee</th>
            <th>Edit</th>
            <th>Delete</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var employee in Model.EmployeesList)
        {
            <tr>
                <td>@Html.ActionLink(employee.LastName + " " + employee.FirstName, "GetEmployeeByName", "Employees", new { lastName = employee.LastName, firstName = employee.FirstName }, null)</td>
                <td>@Html.ActionLink("Edit", "EditEmployee", "Employees", new { lastName = employee.LastName, firstName = employee.FirstName })</td>
                <td>@Html.ActionLink("Delete", "DeleteEmployee", "Employees", null)</td>
            </tr>
        }
    </tbody>
</table>

@section scripts
{
    <script>
        $(document).ready(function () {
            var table = $("employees").DataTable();
        })
    </script>
}